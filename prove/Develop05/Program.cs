// Showed creativity in GoalManager class. I added a function called GetRank(). This looks at the users score and determines what rank they are based on that score, and outputs it to the console. 
using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();

        goalManager.Start();
    }
}